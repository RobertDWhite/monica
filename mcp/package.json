{
  "name": "monica-mcp",
  "version": "0.1.0",
  "description": "MCP server for Monica (personal CRM) — lets Claude read and update your contacts on your behalf.",
  "type": "module",
  "bin": {
    "monica-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
